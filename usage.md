# 飞书机器人使用说明

这份文档面向飞书里的普通使用者。你只需要在机器人所在的群聊或私聊里发送文本消息，机器人会在同一个会话里回复。

## 创建会议

发送以 `创建会议` 开头的消息，可以创建路演或直播会议。创建成功后，机器人会返回会议标题、会议 ID 和观看链接；如果卡片里有“打开会议”按钮，也可以直接点击进入。

最简单的用法：

```text
创建会议
创建会议 AI策略会
```

你也可以把会议参数写成自然语言：

```text
创建会议 明天10点开视频路演，主题是AI策略会，权限专场活动，时长60分钟，直播类型上麦直播
创建会议 2026-06-10 10:00 路演主题AI策略会 视频时长60分钟
```

可识别的信息包括：

- 会议主题：例如 `主题是AI策略会`、`路演主题AI策略会`。
- 开始时间：支持 `2026-06-10 10:00` 这类明确时间，也支持 `明天10点`、`30分钟后` 这类相对时间。
- 路演方式：`无直播`、`音频路演`、`视频路演`。
- 视频时长：例如 `时长60分钟`、`视频时长60分钟`。
- 直播类型：例如 `上麦直播`、`电话会议直播`、`线下直播`、`上传回放`、`文字路演`、`其他`。
- 路演权限：`公开`、`专场活动`、`申请参会`、`金融课堂`、`投资调研`、`私密`、`专栏`、`付费`。

如果没有写主题，默认主题为 `会议`。如果没有写开始时间，默认会创建一个很快开始的会议。

## 创建云播

创建会议时在消息末尾加上 `云播`，可以同时创建视频云播：

```text
创建会议 AI策略会 云播
创建会议 AI策略会 云播 https://example.com/video.mp4
```

创建音频云播时，用 `音频云播` 并提供公网音频地址：

```text
创建会议 AI策略会 音频云播 https://example.com/audio.mp3
```

云播会跟随会议创建流程一起处理。即使云播创建失败，会议本身仍可能创建成功，机器人会在回复里单独提示云播失败原因。

## 查询 Cursor Token 用量

发送 `查询token` 可以查询 Cursor Token 用量。默认查询上月 26 日到当天的用量：

```text
查询token
查询 token
```

也可以指定开始日期和结束日期，日期格式为 `YYYY-MM-DD`：

```text
查询token 2026-05-06 2026-06-04
查询 token 2026-05-06 2026-06-04
```

机器人会回复时间范围、记录数、输入 Tokens、输出 Tokens、缓存读取 Tokens 和合计 Tokens。如果日期格式不正确，或开始日期晚于结束日期，机器人会回复正确用法提示。

## 常见提示

- 命令文字要完整发送到机器人所在的飞书会话里，不需要加斜杠。
- `创建会议` 必须放在消息开头；普通消息不会触发创建会议流程。
- `查询token` 可以写成 `查询 token`，日期范围需要同时提供开始日期和结束日期。
- 如果机器人回复“创建会议失败”，请把失败信息发给机器人维护者排查。
